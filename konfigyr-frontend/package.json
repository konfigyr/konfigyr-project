{
  "name": "konfigyr-frontend",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@11.16.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "test:ui": "turbo run test:ui"
  },
  "devDependencies": {
    "turbo": "^2.10.12"
  },
  "overrides": {
    "chokidar": "^5.0.0"
  }
}
