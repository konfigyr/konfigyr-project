spring:
  liquibase:
    change-log: classpath:namespace-test-changelog.xml

konfigyr:
  invitations:
    cleanup:
      cron: '-'

logging:
  level:
    com.konfigyr: DEBUG
    org.jooq.tools.LoggerListener: DEBUG
